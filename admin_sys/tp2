create rooter -> generic router-PT
create laptop (end device) -> generic laptop PT
connection -> copper cross over
	   -> laptor /fastEternet0

click router:
      (Physical)
      -> turn off
      -> PT-ROUTER-MM-1CFE drag/drop
      -> turn on
      (CLI)
      no
      enter
      enable
      config t
      interface f0/0
      no shut
exit



